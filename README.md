# 📚 EPUB Auto-Translator (Java)

![Java](https://img.shields.io/badge/Java-17%2B-orange)
![Maven](https://img.shields.io/badge/Maven-3.x-blue)
![License](https://img.shields.io/badge/License-MIT-green)
![Platform](https://img.shields.io/badge/Platform-Windows%20%7C%20Linux%20%7C%20macOS-lightgrey)

Простой и мощный инструмент для автоматического перевода книг в формате **EPUB** с английского на русский (или другие языки), используя Google Translate API.

Сохраняет структуру книги, заголовки и форматирование, меняя только текст.

## ✨ Возможности


* **GUI на Swing:** Просто перетащите файл (Drag & Drop) в окно программы.
* **Сохранение верстки:** Использует `jsoup` для аккуратного разбора HTML внутри EPUB.
* **Умная обработка:** Склеивает предложения в пакеты для улучшения контекста перевода.
* **Anti-Ban система:**

  * Имитация реального браузера (User-Agent).
  * Случайные задержки между запросами.
  * Автоматические повторы (Retry) при сбоях сети.
* **Безопасный режим:** Однопоточная обработка для минимизации риска бана.

---

## ⚠️ ВАЖНО: Google API и Блокировки (Error 429)

Эта программа использует **бесплатный** интерфейс Google Translate. У Google есть лимиты, и если переводить слишком быстро, ваш IP может получить временный бан (Ошибка 429).

### ℹ️ Поведение Google при превышении лимитов

На практике Google **не вводит жёстких блокировок** и не "банит" аккаунты.

При превышении лимитов происходит следующее:

* HTTP-запрос **продолжает открываться** (ссылка доступна).
* В ответе приходит **пустая выдача** — `null` / пустой `txt` без перевода.
* Ошибка может выглядеть как `Error 429`, но фактически это **мягкий троттлинг**, а не бан.

⏳ В этом состоянии **лучше просто подождать** — через некоторое время перевод снова начинает работать.

🌍 **Использование VPN** — самый быстрый способ восстановить нормальную выдачу (смена IP решает проблему мгновенно).

❗ Ограничения применяются **к IP-адресу**, а не к аккаунту Google.

### 🛡️ Как избежать бана (Рекомендации):

1. **Используйте VPN!** Это самый надежный способ.

   * Если получили ошибку 429 — просто смените страну в VPN.
   * Лучше менять локацию каждые 30–50 минут перевода большой книги.

2. **Не торопитесь.** В коде специально прописаны задержки (`Thread.sleep`). Не уменьшайте их, если не уверены в своем IP.

3. **Динамический IP.** Если у вас нет VPN, часто помогает перезагрузка роутера для смены IP-адреса.
4. 
---

## 🔍 IP Check Utility

В проекте уже есть тестовый класс `CheckBan` в папке с тестами (`src/test/java/com/translator/CheckBan.java`). Он позволяет проверить статус вашего IP перед переводом:

* Проверяет доступность Google Translate.
* Показывает коды ответа (200, 403, 429 и другие).
* Подсказывает действия при троттлинге или блокировке.

Использование:

```bash
# Запуск теста через Maven
mvn test -Dtest=com.translator.CheckBan
```

Или прямо из IDE: запустите класс `CheckBan` в тестовой папке.

---

## 🛠️ Настройка перевода (Словарь замен)

Автоматический переводчик часто путает имена собственные, клички или сленг. Чтобы книга читалась идеально, вы можете добавить свои **принудительные замены**.

Откройте файл `src/main/java/com/translator/core/EpubProcessor.java` и найдите блок `CORRECTIONS`.

### Пример кода:

```java
private static final Map<String, String> CORRECTIONS = new LinkedHashMap<>();
static {
    // Формат: CORRECTIONS.put("Оригинал или кривой перевод", "Как надо");
    
    // Имена и прозвища
    CORRECTIONS.put("австралиец", "Осси");
    CORRECTIONS.put("Австралиец", "Осси");
    CORRECTIONS.put("VIX", "Викс");
    
    // Термины и сленг
    CORRECTIONS.put("сабвуфер", "саб"); // Если в книге sub — это сокращение от dom/sub, а не колонка :)
    CORRECTIONS.put("Сабвуфер", "Саб");
    
    // Исправление падежей (грубый метод)
    CORRECTIONS.put("сабвуфера", "саба");
    CORRECTIONS.put("сабвуферу", "сабу");
}
```

### 🤖 Лайфхак: Как заполнить словарь с помощью ИИ

Вам не нужно писать это вручную! Скопируйте 1–2 главы книги (или список персонажей) и попросите ChatGPT/Gemini сгенерировать этот код для вас.

**Промпт для ИИ:**

> «Я перевожу книгу [Название книги] с английского на русский программно.
> В книге есть специфические имена и термины: [Перечислить: Vix, Aussie, Sub и т.д.].
> Google Translate переводит их неправильно (например, Aussie как “австралиец”, а надо “Осси”).
> Напиши мне Java-код для наполнения `Map<String, String> CORRECTIONS`, который исправит эти имена, включая варианты с большой/маленькой буквы и основные падежные окончания для русских слов.
> Используй формат: `CORRECTIONS.put("что_заменить", "на_что_заменить");`»

---

## 🚀 Как запустить

### Требования

* Java JDK 17 или выше.
* Maven.

### Сборка и запуск

1. Клонируйте репозиторий.
2. После того как обновили файл `pom.xml` (он уже есть в проекте), выполните в терминале команду, чтобы подтянуть все библиотеки:

```bash
mvn dependency:resolve
```

2. Соберите проект:

```bash
mvn clean package -Dmaven.test.skip=true
```

3. Запустите полученный JAR файл (или через IDE класс `Main`):

```bash
java -jar target/epub-translator-1.0.jar
```

4. Откроется окно. Просто перетащите ваш `.epub` файл в область загрузки.
5. Следите за логами в окне. Готовый файл появится рядом с оригиналом с суффиксом `_RU.epub`.

---

## 📦 Используемые библиотеки

* **Epublib** — чтение и запись EPUB.
* **Jsoup** — парсинг и обработка HTML.
* **OkHttp** — сетевые запросы к Google.
* **Gson** — разбор JSON-ответов.
* **FlatLaf** — красивая тёмная тема оформления.

---

## 📄 Лицензия

MIT License. Используйте на свой страх и риск. Автор не несёт ответственности за возможные блокировки со стороны Google.
